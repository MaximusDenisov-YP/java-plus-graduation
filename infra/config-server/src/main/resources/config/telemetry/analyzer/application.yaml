grpc:
  server:
    port: 0

analyzer:
  kafka:
    topic:
      user-actions: stats.user-actions.v1
      event-similarity: stats.events-similarity.v1

recommendations:
  recent-interactions-limit: 10
  candidate-limit: 20
  nearest-neighbors-limit: 5